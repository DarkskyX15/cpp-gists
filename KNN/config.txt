# 启用开始时命令
allowStartCommand=false
# 每次执行包含的最多命令数
maxLinePerCommand=-1
# 开始时命令的路径
startCommandFile=path/of/command_file
# 多线程数，小于等于0时不使用多线程
multiThreadCount=20
# 启用细节打印
useDetailedPrint=false
# 启用K图表绘制
useRangedDiagram=true
# 图表高度
diagramHeight=10
# 在Windows上将编码调整为Unicode
windowsUnicode=true
# 不进入交互模式
noInteraction=false
